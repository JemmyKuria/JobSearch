// Header.js

import React from 'react';
import './index.css'; // Import the component-specific CSS file

const Header = () => {
  return (
    <header className="header">
      <div className="container">
        <h1>Job Board</h1>
        {/* Add navigation links or any other header content */}
      </div>
    </header>
  );
}

export default Header;
